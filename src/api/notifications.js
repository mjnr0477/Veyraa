import { db } from './firebase';import { collection, addDoc, query, where, getDocs, orderBy } from 'firebase/firestore';export async function addNotification(uid,type,fromUid,postId=null){await addDoc(collection(db,'notifications'),{uid,type,fromUid,postId,createdAt:new Date().toISOString(),read:false});}export async function getNotifications(uid){const q=query(collection(db,'notifications'),where('uid','==',uid),orderBy('createdAt','desc'));const querySnapshot=await getDocs(q);return querySnapshot.docs.map(doc=>({id:doc.id,...doc.data()}));}
