import { db } from './firebase';import { doc, updateDoc, arrayUnion, arrayRemove, getDoc } from 'firebase/firestore';import { addNotification } from './notifications';export async function followUser(myUid,targetUid){await updateDoc(doc(db,'users',myUid),{following:arrayUnion(targetUid)});await updateDoc(doc(db,'users',targetUid),{followers:arrayUnion(myUid)});await addNotification(targetUid,'follow',myUid);}export async function unfollowUser(myUid,targetUid){await updateDoc(doc(db,'users',myUid),{following:arrayRemove(targetUid)});await updateDoc(doc(db,'users',targetUid),{followers:arrayRemove(myUid)});}
